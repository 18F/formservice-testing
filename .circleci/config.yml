# Circleci config
version: 2.1
orbs:
  newman: postman/newman@0.0.2
jobs:
  SmokeTest: ## These are for dark environment
    docker:
     - image: cimg/base:stable
    steps:
      - newman/newman-run:
          collection: formservice/collections/SmokeTest.json
          folder: Test
      
workflows:
  build:
    jobs:
    - SmokeTest
##
##  nightly:
##    triggers:
##      - schedule:
          # The cron format is:
          # min (0-59) hour (0-23) monthday (1-31) month (1-12) weekday (0-6, 0=Sun) - UTC time
##          cron: "10 14 * * *"
##          filters:
##            branches:
##              only: ScheduleRuns
##    jobs:
##      - FHEO-acceptance-test-Dev
